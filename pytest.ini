[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests (no server required)
    integration: Integration tests (requires live server)
    slow: Slow tests
